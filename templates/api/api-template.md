# API 设计：{{模块名称}}

> 最后更新：{{YYYY-MM-DD}}
> 基础路径：/api/{{base-path}}

---

## {{HTTP方法}} {{路径}}

**描述：** {{功能描述}}

**鉴权：** {{不需要 / Bearer Token / API Key}}

**请求参数：**

```json
{
  "{{field}}": "{{type}} - {{说明}}",
  "{{field}}": "{{type}} - {{说明}}"
}
```

**响应（200 成功）：**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "{{field}}": "{{value}}"
  }
}
```

**错误响应：**

| 状态码 | code | 含义 |
|--------|------|------|
| 400 | 1001 | {{参数错误描述}} |
| 401 | 1002 | 未登录或 token 过期 |
| 403 | 1003 | 无权限 |
| 404 | 1004 | 资源不存在 |
| 500 | 5000 | 服务器内部错误 |

**对应 PRD：** F{{ID}}.{{子ID}}

---

## {{HTTP方法}} {{路径}}

{{重复上述结构添加更多接口}}
