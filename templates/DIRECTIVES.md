# DIRECTIVES - 关键指令持久化

> 严格控制在 2000 字以内
> 最后更新：{{YYYY-MM-DDTHH:MM:SS}}
> 用途：跨会话保持关键约束，防止长对话遗忘

---

## 用户核心指令

> 用户明确要求的、必须在整个项目中遵守的指令

- [U1] {{指令描述，如：所有 API 必须支持分页，默认 page_size=20}}
- [U2] {{指令描述}}

---

## PRD 关键约束摘要

> PRD 中的关键业务约束，详情见对应碎片文件

- [P1] {{约束摘要}} → 详见 prd/feat-{{name}}.md
- [P2] {{约束摘要}} → 详见 prd/feat-{{name}}.md

---

## 需求精粹卡

> 生成于需求确认后 | 跨所有阶段携带 | 严格 ≤500 tokens
> 最后更新：待生成

### 产品目标（一句话）
{{待需求分析完成后自动生成}}

### 核心功能清单
- {{待生成}}

### 绝对约束（不可违反）
- {{待生成}}

### 关键验收标准（必须通过）
- {{待生成}}

### 设计资产摘要
- {{待生成，如有设计截图}}

---

## 技术栈约束

> 项目使用的技术栈和关键技术决策

- 前端：{{技术栈，如 React 19 + TypeScript + Tailwind CSS}}
- 后端：{{技术栈，如 Node.js + Express + PostgreSQL}}
- 测试：{{测试框架，如 Vitest + Playwright}}
- UI 视觉测试：{{启用/禁用 Midscene.js}}

---

## UI 规范要点

> 设计规范的关键约束

- [D1] 主色调：{{颜色值，如 #1A73E8}}
- [D2] 圆角：{{值，如 8px}}
- [D3] {{其他 UI 约束}}

---

## 架构决策摘要

> 重要的架构决策，详情见 design 碎片文件

- [A1] {{决策摘要}} → 详见 design/mod-{{name}}.md
- [A2] {{决策摘要}}

---

## 本次任务特殊要求

> 当前迭代的特殊约束（迭代完成后清空）

- [T1] {{特殊要求}}

---

## 周期性上下文回顾协议

> **触发时机：** 每个模块开发完成后 | 每次阶段转换时（S{N} → S{N+1}）
> **执行动作：** 重新读取本文件，输出以下摘要后继续

**回顾输出格式（简洁版，≤3 行）：**
```
[上下文回顾] 核心约束：{U 指令条数}条 | PRD关键约束：{P 条数}条 | 技术栈：{一句话} | 需求精粹：{精粹卡关键词}
```

> 如发现当前实现方向与上述约束存在偏离，立即暂停并向用户报告。
