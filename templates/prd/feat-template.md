# F{{ID}}: {{功能点名称}}

> 状态：⬜ 待确认 | 优先级：{{P0/P1/P2}} | 创建：{{YYYY-MM-DD}}

---

## 用户故事

> 明确功能的使用者、行为和价值。

**作为** [{{用户角色，如：注册用户 / 管理员 / 访客}}]，
**我希望** [{{期望的行为，如：能够通过邮箱和密码登录系统}}]，
**以便** [{{获得的价值，如：访问我的个人数据和使用受保护的功能}}]。

**前置条件：** {{使用该功能前必须满足的条件，如：用户已完成注册}}

**后置条件：** {{功能执行成功后的系统状态，如：用户登录成功，生成 session}}

---

## 功能描述

{{2-3 段话描述功能目标、解决的问题、业务价值。要求具体、可衡量，不使用模糊词汇。}}

---

## 业务功能流程图

> 每个功能点必须给出主流程（含关键分支），便于评审与测试对齐。**推荐**：ASCII 流程图（纯文本、易 diff）；复杂交互可另附 Mermaid `flowchart`/`sequenceDiagram` 代码块。

**主流程（示例结构，请按实际替换）：**

```text
[开始] → [前置条件检查] → [用户操作/系统动作] → {判断?}
                              ↓ 是              ↓ 否
                         [成功路径]         [异常/分支处理] → [结束]
```

{{在此处写本功能的真实步骤：用 → 连接，用 {菱形?} 表示判断，多分支时对齐缩进}}

---

## 界面原型（ASCII 方框图）

> 凡涉及页面、弹窗、表单布局，必须用 ASCII 方框图画出**区域划分与主要控件**，与设计稿互补；无 UI 的纯后端功能可写「无界面，跳过」并给出接口/消息流示意即可。

**示例（请按实际替换为当前功能的布局）：**

```text
┌─────────────────────────────────────────────────────────────┐
│  {{页面/弹窗标题}}                                    [×]   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌──────────────────────────────┐  │
│  │ {{区域 A：如侧栏}}   │  │ {{区域 B：主内容}}            │  │
│  │                     │  │  [主要按钮]  [次要按钮]       │  │
│  └─────────────────────┘  └──────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  {{底栏：如分页 / 统计}}                                      │
└─────────────────────────────────────────────────────────────┘
```

{{ASCII 原型正文：对齐「功能点明细」中的子功能，关键字段旁可用 (备注) 标注校验规则}}

---

## 功能点明细

> 每条验收标准使用 **Given-When-Then** 格式，确保可测试、可量化。

| # | 子功能 | 验收标准（Given-When-Then） | 优先级 |
|---|--------|---------------------------|--------|
| F{{ID}}.1 | {{子功能名称}} | **Given** {{前置条件}}，**When** {{用户操作/系统事件}}，**Then** {{可量化的预期结果}} | P0 |
| F{{ID}}.2 | {{子功能名称}} | **Given** {{前置条件}}，**When** {{用户操作/系统事件}}，**Then** {{可量化的预期结果}} | P1 |
| F{{ID}}.3 | {{子功能名称}} | **Given** {{前置条件}}，**When** {{用户操作/系统事件}}，**Then** {{可量化的预期结果}} | P1 |

---

## 数据字典

> 涉及的核心数据字段定义。如有数据库表结构，在此定义字段。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| {{字段名，如：user_id}} | {{类型，如：UUID}} | {{约束，如：PK, NOT NULL}} | {{说明，如：用户唯一标识}} |
| {{字段名}} | {{类型}} | {{约束}} | {{说明}} |
| {{字段名}} | {{类型}} | {{约束}} | {{说明}} |

> 如不涉及数据模型（纯前端展示），标注"不适用"。

---

## UI 设计要求

> 从用户描述或设计稿中提取的 UI 要求。

- 布局：{{布局描述}}
- 主要元素：{{关键 UI 元素描述}}
- 交互：{{关键交互行为，如：点击提交按钮后显示 loading 状态，3 秒内返回结果}}
- 响应式：{{移动端/平板/桌面的适配要求，如适用}}
- 无障碍：{{可访问性要求，如适用}}

### 设计资产引用

| 页面/组件 | 设计截图路径 | 说明 |
|-----------|-------------|------|
| {{页面名}} | {{相对路径，如 .codeman/assets/design/login-page.png}} | {{说明}} |

> 如无设计稿，标注"待设计稿确认"。
> 截图文件建议存放在 `.codeman/assets/design/` 目录。
> 支持 png/jpg/svg/figma-export 格式。用户也可提供本地绝对路径。

---

## 业务规则

> 关键业务约束和不可违反的规则。每条规则必须可验证。

- BR{{ID}}.1: {{业务规则1，如：同一邮箱 24 小时内最多发送 5 次验证邮件}}
- BR{{ID}}.2: {{业务规则2，如：密码必须包含大小写字母和数字，长度 8-32 位}}
- BR{{ID}}.3: {{业务规则3}}

---

## 边界场景

> 需要特殊处理的边界情况。每个场景必须明确预期的系统行为。

| # | 边界场景 | 预期系统行为 |
|---|---------|------------|
| 1 | {{场景1，如：网络超时}} | {{预期行为，如：显示"网络异常，请重试"提示，表单数据保留}} |
| 2 | {{场景2，如：重复提交}} | {{预期行为，如：按钮禁用 3 秒，后端做幂等性校验}} |
| 3 | {{场景3，如：数据为空}} | {{预期行为，如：显示空状态插图和引导文案}} |
| 4 | {{场景4，并发操作}} | {{预期行为，如：乐观锁机制，冲突时提示用户刷新}} |

---

## 非功能需求

> 性能、安全、可用性等非功能约束。如不适用可标注"N/A"。

### 性能指标

| 指标 | 要求 | 说明 |
|------|------|------|
| {{指标1，如：API 响应时间}} | {{要求，如：P95 < 200ms}} | {{说明}} |
| {{指标2，如：页面加载时间}} | {{要求，如：LCP < 2.5s}} | {{说明}} |
| {{指标3，如：并发用户数}} | {{要求，如：支持 1000 并发}} | {{说明}} |

### 安全要求

| 要求 | 说明 |
|------|------|
| {{安全要求1，如：密码 bcrypt 加密存储}} | {{说明}} |
| {{安全要求2，如：API 请求需 JWT 鉴权}} | {{说明}} |

### 可用性要求

| 要求 | 说明 |
|------|------|
| {{可用性要求1，如：支持键盘导航}} | {{说明}} |
| {{可用性要求2，如：错误提示需同时包含图标和文字}} | {{说明}} |

---

## 依赖关系

- 依赖功能点：{{F{ID}（如有）}}
- 依赖外部服务：{{第三方服务（如有），如：邮件服务 SendGrid、短信服务 Twilio}}
- 依赖基础设施：{{如：Redis 缓存、S3 存储}}

---

## 假设与约束

> 需求分析时的假设条件和技术约束，帮助后续开发理解上下文。

**假设：**
- {{假设1，如：假设用户已在浏览器中启用 JavaScript}}
- {{假设2，如：假设第三方邮件服务 SLA ≥ 99.9%}}

**约束：**
- {{约束1，如：必须兼容 Chrome 90+、Safari 14+、Firefox 88+}}
- {{约束2，如：数据库仅支持 PostgreSQL 14+}}

---

## 变更历史

| 日期 | 变更内容 | 变更人 |
|------|---------|--------|
| {{YYYY-MM-DD}} | 初始创建 | AI |
