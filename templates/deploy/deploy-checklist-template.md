# 部署清单

> 生成时间：{{YYYY-MM-DDTHH:MM:SS}}
> 基准版本：{{上一个 tag}}
> 当前版本：{{当前 Git commit hash}}
> 变更功能点：{{F001, F002}}

---

## 部署前检查

- [ ] 所有测试通过（L1-L4）
- [ ] Review 已通过
- [ ] 数据库备份已完成
- [ ] 回滚方案已准备

---

## 1. 环境变量变更

### 新增
| 变量名 | 示例值 | 说明 | 必填 |
|--------|--------|------|------|
| {{VAR_NAME}} | {{示例}} | {{说明}} | ✅ |

### 修改
| 变量名 | 旧值说明 | 新值说明 |
|--------|---------|---------|
| （无）| - | - |

### 删除
| 变量名 | 说明 |
|--------|------|
| （无）| - |

---

## 2. 数据库变更

### 执行顺序

**Step 1: 备份**
```bash
# 根据数据库类型选择备份命令
pg_dump {{database_name}} > backup-$(date +%Y%m%d-%H%M%S).sql
```

**Step 2: 执行迁移**
```sql
-- 在此填入迁移脚本
```

**Step 3: 验证**
```sql
-- 在此填入验证查询
```

### 回滚脚本
```sql
-- 如需回滚，执行以下语句
```

---

## 3. 依赖变更

### 新增
| 包名 | 版本 | 用途 |
|------|------|------|
| （无）| - | - |

### 升级
| 包名 | 旧版本 | 新版本 | 原因 |
|------|--------|--------|------|
| （无）| - | - | - |

---

## 4. 部署步骤

- [ ] **Step 1: 拉取代码**
  ```bash
  git pull origin main
  ```

- [ ] **Step 2: 安装依赖**
  ```bash
  {{安装命令}}
  ```

- [ ] **Step 3: 数据库迁移**（如有）
  参见"数据库变更"章节

- [ ] **Step 4: 更新环境变量**（如有）
  参见"环境变量变更"章节

- [ ] **Step 5: 构建**
  ```bash
  {{构建命令}}
  ```

- [ ] **Step 6: 重启服务**
  ```bash
  {{重启命令}}
  ```

- [ ] **Step 7: 验证**
  - [ ] 健康检查：{{健康检查 URL}} 返回 200
  - [ ] 核心功能验证：{{验证步骤}}

---

## 5. API 变更

### 新增端点
| 方法 | 路径 | 说明 |
|------|------|------|
| （无）| - | - |

### 修改端点（注意兼容性）
| 方法 | 路径 | 变更说明 | 兼容性 |
|------|------|---------|--------|
| （无）| - | - | - |

---

## 6. 风险提示

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| （无）| - | - |

### 回滚方案

如部署失败：
1. 回滚代码：`git checkout {{上一个 tag}}`
2. 回滚数据库（如有）：执行"数据库变更"中的回滚脚本
3. 重启服务
