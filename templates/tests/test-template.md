# 测试用例：{{功能点名称}}

> PRD 来源：F{{ID}} | 创建：{{YYYY-MM-DD}}

---

## TC-{{ID}}-L2-{{NN}}: {{测试场景名称（集成测试）}}

**测试层级：** L2 集成测试

**PRD 来源：** F{{ID}}.{{子ID}} — {{验收标准原文}}

**测试类型：** API 接口 / 数据库交互 / 前后端联调

**前置条件：**
- {{前置条件1，如：数据库中有测试数据}}
- {{前置条件2，如：用户 token 已准备}}

**测试步骤：**
1. {{操作步骤1，如：POST /api/register 发送注册请求}}
2. {{操作步骤2，如：验证响应体包含 user_id 和 token}}
3. {{操作步骤3，如：查询数据库确认用户记录已创建}}

**预期结果：** {{预期的系统响应}}

**断言要点（直接对应验收标准）：**
- [ ] {{具体可验证的断言1，如：返回 201 状态码}}
- [ ] {{具体可验证的断言2，如：数据库中 user_email 字段正确}}

**执行状态：** ⬜ 未执行 / ✅ 通过 / ❌ 失败

---

## TC-{{ID}}-L3-{{NN}}: {{测试场景名称（E2E 端到端）}}

**测试层级：** L3 E2E 端到端

**PRD 来源：** F{{ID}}.{{子ID}} — {{验收标准原文}}

**前置条件：**
- {{前置条件1，如：用户已登录}}
- {{前置条件2}}

**测试步骤：**
1. {{操作步骤1，如：导航到注册页面}}
2. {{操作步骤2，如：填写邮箱和密码}}
3. {{操作步骤3，如：点击注册按钮}}

**预期结果：** {{预期的系统响应}}

**断言要点（直接对应验收标准）：**
- [ ] {{具体可验证的断言1，如：页面显示"注册成功"提示}}
- [ ] {{具体可验证的断言2，如：用户邮箱出现在用户列表中}}

**执行状态：** ⬜ 未执行 / ✅ 通过 / ❌ 失败

---

## TC-{{ID}}-L4-{{NN}}: {{测试场景名称（UI 视觉）}}

**测试层级：** L4 UI 视觉测试

**PRD 来源：** F{{ID}} — {{UI 设计要求原文}}

**PRD UI 要求：**
- {{UI 要求1，如：注册表单居中显示，宽度 400px}}
- {{UI 要求2，如：主按钮颜色为蓝色 #1A73E8}}

**设计截图：** {{截图路径，如：.codeman/assets/design/register.png，无则标注"无设计稿"}}

**Midscene.js 断言：**
```javascript
await agent.aiAssert('{{自然语言描述的 UI 断言，直接对应 PRD UI 要求}}');
```

**断言要点（验证 UI 与设计要求一致）：**
- [ ] {{具体可验证的视觉断言1，如：表单宽度约 400px，居中显示}}
- [ ] {{具体可验证的视觉断言2，如：按钮背景色为 #1A73E8}}

**执行状态：** ⬜ 未执行 / ✅ 通过 / ❌ 失败

---

## TC-{{ID}}-L5-{{NN}}: {{测试场景名称（非功能）}}

**测试层级：** L5 非功能测试

**测试类型：** 性能 / 安全 / 可访问性

**PRD 来源：** {{相关 PRD 约束原文，如：API 响应时间 < 200ms}}

**测试命令：**
```bash
{{测试命令，如：npx lighthouse http://localhost:3000 --output=json}}
```

**阈值：**
- {{具体阈值，如：API 响应时间 P95 < 200ms}}
- {{具体阈值，如：Lighthouse 性能评分 ≥ 80}}

**断言要点：**
- [ ] {{具体可验证的断言1，如：P95 延迟 < 200ms}}
- [ ] {{具体可验证的断言2，如：0 个高危漏洞}}

**执行状态：** ⬜ 未执行 / ✅ 通过 / ❌ 失败

---

## TC-{{ID}}-L2-{{NN}}: {{测试场景名称（边界/异常 — 集成测试）}}

**测试层级：** L2 集成测试

**PRD 边界场景对照：** {{边界场景原文，如：邮箱格式不正确}}

**前置条件：**
- {{前置条件}}

**测试步骤：**
1. {{操作步骤，如：POST /api/register 发送邮箱格式错误的请求}}

**预期结果：** {{预期的系统行为，如：返回 400 + 错误信息 "邮箱格式不正确"}}

**断言要点（验证边界场景的预期系统行为）：**
- [ ] {{具体可验证的断言，如：返回 400 状态码}}
- [ ] {{具体可验证的断言，如：错误信息包含"邮箱格式不正确"}}

**执行状态：** ⬜ 未执行 / ✅ 通过 / ❌ 失败
