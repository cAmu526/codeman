# CodeMan v0.3

> Cursor IDE 上的全流程开发工作流框架。用自然语言驱动 AI 完成从需求分析到部署的全链路开发。

---

## 它能帮你做什么

CodeMan 是一套安装在 Cursor 上的 AI 工作流框架。安装后，你只需在 Cursor 的 Agent 对话框里用自然语言下指令，AI 就会自动按照**需求分析 → 技术方案 → 编码实现 → 测试验证 → 部署清单**的完整流程帮你开发。

**核心价值：**

- **文档驱动**：每一步都产出结构化文档（PRD、技术方案、API 设计、测试用例、部署清单），AI 跨会话也不会丢失上下文
- **质量内建**：编码前后自动对照需求，Review 门禁拦截问题，测试基准来源于 PRD 而非代码
- **断点续做**：关掉 Cursor 再打开，说一句 `CodeMan 继续` 就能从上次的进度接着干
- **新旧项目通吃**：全新项目从零开始，已有代码库也能快速接入

---

## 快速开始

### 第一步：安装（一次性，约 10 秒）

```bash
bash /path/to/codeman/install.sh
```

> 将 `/path/to/codeman/` 替换为你实际存放 `install.sh` 的路径。

安装会做两件事：
1. 把框架复制到 `~/.cursor/skills/.codeman/`（全局可用，不污染业务项目）
2. 在 `~/.cursor/rules/` 生成引导规则，让 Cursor AI 能识别 CodeMan 命令

**安装完成后重启 Cursor。**

### 第二步：在项目中初始化

用 Cursor 打开你的项目，在 Agent 对话框中说：

```
CodeMan 初始化
```

AI 会自动运行初始化脚本，交互式询问你的项目信息（项目名、技术栈、前后端框架等），然后在项目根目录生成 `.codeman/` 文档体系。

**初始化会根据项目情况走不同路径：**

| 项目情况 | AI 的处理方式 |
|---------|-------------|
| **全新项目**（目录里没有业务代码） | 自动进入需求分析，引导你梳理功能点，生成 PRD |
| **已有代码库 — 完整接入** | 扫描全部代码，反向提炼 PRD + 技术方案 + API 文档（适合 ≤20 个模块的中小项目，耗时 1-3 小时） |
| **已有代码库 — 骨架接入** | 只读目录结构和配置文件，5-10 分钟生成骨架，后续开发时自动按需补全文档（推荐大型项目使用） |

### 第三步：开始开发

初始化完成后，根据你的需要说不同的命令：

```
CodeMan 开始开发
```

AI 就会自动走完整开发流程。关键节点会暂停等你确认，不会自作主张。

---

## 所有命令一览

| 命令 | 什么时候用 | AI 会做什么 |
|------|-----------|------------|
| `CodeMan 初始化` | 项目第一次使用 CodeMan | 生成 `.codeman/` 文档体系，检测技术栈，安装编码规范 |
| `CodeMan 开始开发` | 初始化后启动开发 | 按流程走完：需求 → 方案 → 编码 → 测试 → 部署 |
| `CodeMan 新需求：[描述]` | 需要加新功能或改需求 | 增量需求分析 + 影响评估，然后走开发流程 |
| `CodeMan 继续` | 上次没做完，接着干 | 读取进度文件，从断点处继续 |
| `CodeMan 修复：[描述]` | 修 Bug 或小改动 | 定位 → 修复 → 回归测试 → 文档同步（3-10 分钟搞定） |
| `CodeMan 状态` | 想看看当前进度 | 展示当前阶段、已完成/待完成的功能点、遗留问题 |
| `CodeMan 概览` | 查看/更新项目概览 | 汇总所有碎片文档，生成面向新成员的一站式项目概览 |

---

## 完整工作流详解

说完命令后，AI 自动识别场景并按以下流程执行：

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ 需求分析 │ → │ 技术方案 │ → │ 开发实现 │ → │ 测试验证 │ → │ 部署清单 │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
                    ↓               ↓              ↓
               Review 门禁     Review 门禁    失败 → 修复闭环
                                               (最多 3 轮自动修复)
```

### 1. 需求分析

AI 通过六维头脑风暴帮你拓展需求，然后收敛为结构化的 PRD 功能点碎片文件。

- **输入**：你的需求描述（一句话或一段话都行）
- **过程**：拓展 → 红/黄/绿分类 → 你确认保留哪些 → 生成文档
- **产出**：`.codeman/docs/prd/feat-*.md`（每个功能点一个文件）

### 2. 技术方案

基于确认的 PRD，AI 做模块拆分和技术方案设计。

- **输入**：PRD 功能点 + 项目配置
- **过程**：模块边界划分 → 每模块技术选型与数据模型 → API 接口设计 → **自动 Review 门禁**
- **产出**：`.codeman/docs/design/mod-*.md` + `.codeman/docs/api/api-*.md`

### 3. 开发实现

按技术方案逐模块编码，编码前后强制对照需求。

- **编码前**：输出 PRD + UI + 技术方案的对照清单，你确认后才开始
- **编码中**：禁止凭推测补充 PRD 未提及的逻辑，遇到歧义暂停问你
- **编码后**：逐条输出需求实现对照表 → **自动 Review 门禁** → Git commit
- **同步产出**：单元测试（覆盖率 ≥ 80%）

### 4. 测试验证

执行五层测试金字塔，测试基准来源于 PRD（不是代码）：

| 层级 | 测试类型 | 说明 |
|------|---------|------|
| L1 | 单元测试 | 编码时已同步生成，此阶段验证并补充 |
| L2 | 集成测试 | API 接口 + 数据库交互 |
| L3 | E2E 端到端 | 每条 PRD 验收标准至少 1 个 E2E 用例，支持 Playwright MCP 自动执行 |
| L4 | UI 视觉测试 | 使用 Midscene.js 自然语言断言（可选） |
| L5 | 非功能测试 | 性能 / 安全 / 可访问性（可选） |

测试全部通过 → 进入部署清单；有失败 → 自动进入修复闭环（最多 3 轮）。

### 5. 修复闭环

测试失败时自动触发，也可手动用 `CodeMan 修复：[描述]` 触发。

- 根因分析 → 影响评估 → 修复 → 回归验证 → 文档同步
- 最多自动修复 3 轮，修不好会停下来让你介入

### 6. 部署清单

所有测试通过后自动生成，包含：环境变量、数据库迁移、依赖变更、部署步骤、API 变更说明、风险与回滚方案。

### 7. 自进化（自动触发）

Review 中发现同类问题出现 ≥ 2 次时，AI 会自动草拟新的编码规范（Rule）或工作流技能（Skill），经你确认后写入项目，后续开发自动遵守。

---

## 项目中会生成什么

初始化后，项目根目录会多出 `.codeman/` 目录：

```
.codeman/
├── config.yaml                 ← 项目配置（技术栈、测试框架、Token 阈值等）
├── docs/
│   ├── PROJECT-OVERVIEW.md     ← 项目概览（新成员从这里开始，5 分钟了解全貌）
│   ├── STATUS.md               ← 当前开发进度（AI 跨会话的记忆）
│   ├── DIRECTIVES.md           ← 关键决策和约束（跨会话持久化）
│   ├── prd/                    ← PRD 功能点碎片
│   │   ├── INDEX.md            ← 功能点索引（ID、状态、优先级）
│   │   └── feat-*.md           ← 每个功能点的详细 PRD
│   ├── design/                 ← 技术方案
│   │   ├── INDEX.md            ← 模块索引
│   │   ├── arch-overview.md    ← 架构总览
│   │   └── mod-*.md            ← 每个模块的技术方案
│   ├── api/                    ← API 设计
│   │   ├── INDEX.md            ← 接口列表索引
│   │   └── api-*.md            ← 每个接口的详细设计
│   ├── tests/                  ← 测试文档
│   │   ├── INDEX.md            ← 测试索引 + 需求覆盖率矩阵
│   │   ├── test-*.md           ← 测试用例文档
│   │   └── test-report-latest.md ← 最新测试报告
│   ├── reviews/                ← Review 报告
│   │   ├── INDEX.md            ← Review 索引
│   │   └── review-*.md         ← 各阶段 Review 报告
│   └── deploy/                 ← 部署清单
│       ├── INDEX.md            ← 部署清单索引
│       └── deploy-checklist.md ← 部署实操清单
├── rules/                      ← 项目编码规范（自动同步到 .cursor/rules/）
└── templates/                  ← 文档模板（供 AI 生成文档时参照）
```

> `PROJECT-OVERVIEW.md` 是面向人类新成员的一站式概览，包含项目简介、技术架构、功能地图、模块结构、API 概览、关键决策、开发规范、当前进度和上手指南。每个开发阶段完成后自动更新，也可随时说 `CodeMan 概览` 手动刷新。

> 这些文档是 AI 跨会话工作的「记忆」，**建议提交到 Git**。

---

## 内置编码规范

初始化时会根据检测到的技术栈自动安装对应规范：

**全局规范（所有项目）：**
- Git 提交规范（Conventional Commits、分支命名、禁止直推 main）
- 通用代码质量（命名、函数设计、错误处理）
- 安全基线（密钥管理、输入校验、XSS 防护）

**技术栈规范（按需安装）：**

| 技术栈 | 规范内容 |
|--------|---------|
| React | 组件目录结构、Props 类型、状态管理 |
| Next.js | App Router 约定、RSC/Client 组件划分 |
| Vitest | 测试结构、语义化查询、Mock 规范 |
| Node.js/Express | 分层架构、async/await、错误中间件 |
| Python/FastAPI | Pydantic 类型、依赖注入、异步约束 |
| Go | 错误处理包装、命名规范、并发与 context |

其他技术栈（Vue、Svelte、Java、Rust 等）：初始化时生成 `PENDING-RULES.md`，首次开发时 AI 自动生成专属规范。

---

## 各场景预估成本

| 场景 | 适用情况 | 预估耗时 | Token 消耗 |
|------|---------|---------|-----------|
| 全新项目 | 从零开始 | 1-4 小时 | ~180K |
| 旧项目完整接入 | 中小型代码库（≤ 20 模块） | 1-3 小时 | ~50K-100K |
| 旧项目骨架接入 | 大型代码库 | 5-10 分钟 | ~3K-5K |
| 版本迭代 | 新需求 / 功能变更 | 30 分钟 - 2 小时 | ~50K-135K |
| 轻量修复 | Bug / 小改动 | 3-10 分钟 | ~8K-15K |

---

## 可选的外部工具

以下工具不是必须的，但安装后可以增强测试能力：

| 工具 | 用途 | 安装方式 |
|------|------|---------|
| Playwright MCP | L3 E2E 测试自动执行 | `npm install -g @playwright/mcp` |
| Midscene.js | L4 UI 视觉测试（自然语言断言） | `npm install @midscene/web` |

---

## 升级与卸载

### 升级框架

在 CodeMan **源码目录**下执行（不是安装目录）：

```bash
bash /path/to/codeman/update.sh
```

升级只更新框架文件，不会覆盖各项目的 `.codeman/docs/` 文档和项目级规范。升级后重启 Cursor 生效。

### 全局卸载

彻底移除 CodeMan 全局框架：

```bash
bash ~/.cursor/skills/.codeman/uninstall.sh
```

卸载后重启 Cursor 生效。各业务项目中的 `.codeman/` 目录不受影响。

### 项目反初始化

只清理当前项目的 CodeMan 配置（规范、模板），保留 `.codeman/docs/` 文档资产：

```bash
# 在项目根目录执行
bash ~/.cursor/skills/.codeman/uninit.sh
```

---

## 常见问题

**Q：说了命令但 AI 没反应？**
A：确认已重启 Cursor。检查 `~/.cursor/rules/codeman-bootstrap.mdc` 文件是否存在。

**Q：换了一个 Cursor 会话，AI 还记得之前的进度吗？**
A：记得。所有进度保存在项目的 `.codeman/docs/STATUS.md` 中，新会话说 `CodeMan 继续` 即可恢复。

**Q：可以跳过某个阶段吗？**
A：流程中的关键节点都会暂停确认，你可以在确认时告诉 AI 跳过或调整。轻量修复场景（`CodeMan 修复`）会自动跳过不必要的阶段。

**Q：多人协作怎么用？**
A：将 `.codeman/` 目录提交到 Git，团队成员各自安装 CodeMan 框架后，打开项目说 `CodeMan 继续` 即可基于同一份文档协作。

**Q：新人接手项目，从哪里开始了解？**
A：打开 `.codeman/docs/PROJECT-OVERVIEW.md`，5 分钟即可了解项目全貌（业务目标、技术架构、功能地图、模块结构、上手指南）。如果文档不是最新的，在 Cursor 中说 `CodeMan 概览` 即可刷新。

**Q：PROJECT-OVERVIEW.md 和其他文档是什么关系？**
A：`PROJECT-OVERVIEW.md` 是面向人类的「导航页」，从各碎片文档（PRD、技术方案、API 等）中汇总关键信息，每个条目都附有详情链接。它在每个开发阶段完成后自动更新，不需要手动维护。

**Q：支持哪些 IDE？**
A：目前仅支持 Cursor IDE。

---

## 框架目录结构

```
~/.cursor/skills/.codeman/        ← 全局安装位置（不放进业务项目）
├── install.sh                    ← 安装脚本
├── init.sh                       ← 项目初始化脚本（由 AI 调用）
├── update.sh                     ← 升级框架
├── uninstall.sh                  ← 全局卸载
├── uninit.sh                     ← 项目反初始化
├── skills/                       ← 9 个核心工作流技能
│   ├── orchestrator/SKILL.md     ← 主编排入口（识别场景、调度流程）
│   ├── requirements/SKILL.md     ← 需求分析（头脑风暴 → PRD 碎片）
│   ├── design/SKILL.md           ← 技术方案（模块拆分 + API 设计）
│   ├── development/SKILL.md      ← 开发实现（对照编码 + 同步单测）
│   ├── testing/SKILL.md          ← 测试验证（L1-L5 金字塔）
│   ├── review/SKILL.md           ← Review 门禁（自动审查 + 问题修复）
│   ├── fix/SKILL.md              ← 修复闭环（根因分析 + 回归验证）
│   ├── deploy/SKILL.md           ← 部署清单（环境 + 迁移 + 回滚）
│   └── evolve/SKILL.md           ← 自进化（沉淀规范 + 动态创建规则）
├── rules/                        ← 内置编码规范模板
├── templates/                    ← 文档模板
└── adapters/cursor/              ← Cursor IDE 适配脚本
```

---

## 版本历史

- **v0.3.1** — 2026-03-30：新增 PROJECT-OVERVIEW.md 项目概览文档，面向新成员一站式了解项目全貌；新增 `CodeMan 概览` 命令；各阶段完成后自动更新概览
- **v0.3** — 2026-03-24：旧项目接入新增骨架模式；开发 Skill 新增按需文档化；E2E 测试升级为逐条验收标准对应；测试 Review 新增验收标准逐条对照
- **v0.2** — 2026-03-24：重构安装方式，新增一键安装；bootstrap rule 全局生效；旧项目接入补全技术文档反向提炼
- **v0.1** — 2026-03-23：init.sh 自动检测技术栈；旧项目接入新增反向提炼 PRD
- **v0.0** — 2026-03-23：初始版本，完整实现全流程
